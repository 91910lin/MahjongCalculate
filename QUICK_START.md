# 🚀 快速啟動指南

## 第一次使用

### 1. 安裝依賴
在專案根目錄執行：
```bash
npm install
```

### 2. 啟動開發伺服器
```bash
npm run dev
```

### 3. 開啟瀏覽器
訪問顯示的本地網址（通常是 `http://localhost:5173`）

## 常用指令

```bash
# 開發模式
npm run dev

# 執行測試
npm test

# 建置生產版本
npm run build

# 預覽建置結果
npm run preview
```

## 部署到 GitHub Pages

### 步驟 1：建立 GitHub 儲存庫
1. 在 GitHub 建立新的儲存庫（例如：`MahjongCalculate`）
2. 不要初始化 README、.gitignore 或 license

### 步驟 2：推送程式碼
```bash
git init
git add .
git commit -m "Initial commit: 台灣麻將計算器"
git branch -M main
git remote add origin https://github.com/你的用戶名/MahjongCalculate.git
git push -u origin main
```

### 步驟 3：設定 GitHub Pages
1. 進入儲存庫的 **Settings** 頁面
2. 點選左側選單的 **Pages**
3. 在 "Build and deployment" 區塊：
   - Source: 選擇 **GitHub Actions**
4. 返回儲存庫首頁，點選 **Actions** 標籤
5. 確認部署工作流程正在執行

### 步驟 4：訪問網站
部署完成後，你的網站將可在以下網址訪問：
```
https://你的用戶名.github.io/MahjongCalculate/
```

## 更新 base 路徑（如果儲存庫名稱不同）

如果你的儲存庫名稱不是 `MahjongCalculate`，需要修改 [vite.config.ts](vite.config.ts)：

```typescript
export default defineConfig({
  plugins: [react()],
  base: process.env.NODE_ENV === 'production' ? '/你的儲存庫名稱/' : '/',
  // ...
})
```

## 疑難排解

### 問題：GitHub Pages 顯示 404
**解決方案**：
- 確認 vite.config.ts 中的 `base` 路徑與儲存庫名稱一致
- 確認 GitHub Pages 設定為使用 GitHub Actions
- 檢查 Actions 標籤，確認部署工作流程成功完成

### 問題：路由刷新後顯示 404
**解決方案**：
- 本專案使用 HashRouter，不會有此問題
- 網址格式會是：`https://.../#/`

### 問題：測試失敗
**解決方案**：
```bash
# 清除快取並重新安裝
rm -rf node_modules package-lock.json
npm install
npm test
```

## 專案特色

✅ 純前端靜態網站（無需後端伺服器）
✅ 完整的台灣麻將規則實作
✅ 支援副露（吃、碰、槓）
✅ 互動式練習模式
✅ 自動部署到 GitHub Pages

## 下一步

- 查看 [README.md](README.md) 瞭解完整功能
- 閱讀原始碼，瞭解核心演算法
- 修改樣式，自訂你的主題
- 添加新功能，貢獻到專案

祝你使用愉快！🀄
